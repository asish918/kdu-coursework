import styled from "styled-components"

const AppHeader = styled.header`
    text-align: center;
    margin-block: 30px;
`

export default function Header() {
    return (
        <AppHeader>
            Hotel Booking
        </AppHeader>
    )
}